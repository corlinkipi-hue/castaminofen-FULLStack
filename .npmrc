# Hoist patterns for React Native / Expo compatibility in monorepo
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*react-native*

# Strict peer deps can break cross-platform monorepos (web=React 19, mobile=React 18)
strict-peer-dependencies=false
